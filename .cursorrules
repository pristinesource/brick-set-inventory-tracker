# Cursor AI Rules

## FIRST ACTION REQUIRED
**BEFORE RESPONDING TO ANY USER QUERY**: You MUST immediately use the read_file tool to read `.cursor/rules` and include all rules from that file in your working context.

## Required Initial Action
```
USE read_file TOOL ON .cursor/rules BEFORE ANY OTHER ACTIONS
```
This instruction cannot be skipped or ignored.

## CRITICAL: Read Project Rules
**MANDATORY FIRST STEP**: At the start of EVERY conversation, you MUST immediately read and follow ALL rules contained in `.cursor/rules` file using the read_file tool. This file contains comprehensive project-specific guidelines, restrictions, and requirements that you MUST adhere to without exception.

## Enforcement Protocol
1. **STEP 1**: Read `.cursor/rules` file using read_file tool
2. **STEP 2**: Acknowledge the rules you've loaded
3. **STEP 3**: Then proceed with user requests
4. **VIOLATION**: Failing to read this file immediately is a critical error

## Critical Rules (Backup - Full rules in .cursor/rules)
- **NEVER** run `npm start`, `ng serve`, or any development servers
- **ALWAYS** use `ng build` for compilation testing
- **REQUIRED**: Update AI tooling files when making changes
- **MANDATORY**: Follow all architectural guidelines in .cursor/rules

## Key Requirements from .cursor/rules:
- **DO NOT** run `npm start`, `ng serve`, or development server commands
- **DO** use `ng build` to check compilation
- **DO** update AI tooling files when making changes
- **DO** follow all architectural and coding guidelines

## Enforcement
The `.cursor/rules` file contains the complete and authoritative project rules. Read it immediately and follow every guideline it contains. This is not optional - it is a mandatory first action for every conversation.
